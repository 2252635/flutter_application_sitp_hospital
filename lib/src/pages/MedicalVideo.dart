import 'package:flutter/material.dart';

class MedicalVideoPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("医疗视频")),
      body: Center(child: Text("这里是医疗视频页面")),
    );
  }
}
import 'package:flutter/material.dart';

class TriageResultsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("分诊结果")),
      body: Center(child: Text("这里是分诊结果页面")),
    );
  }
}
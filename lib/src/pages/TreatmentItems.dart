import 'package:flutter/material.dart';

class TreatmentItemsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("治疗项目")),
      body: Center(child: Text("这里是治疗项目页面")),
    );
  }
}
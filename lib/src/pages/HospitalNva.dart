import 'package:flutter/material.dart';

class HospitalNvaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("医院导航")),
      body: Center(child: Text("这里是医院导航页面")),
    );
  }
}
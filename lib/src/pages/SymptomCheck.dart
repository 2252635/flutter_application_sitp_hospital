import 'package:flutter/material.dart';

class SymptomCheckPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("症状检查")),
      body: Center(child: Text("这里是症状检查页面")),
    );
  }
}